
<p> Welcome, <%= current_user.username %>
<h2> What You're wathcing... </h2>

<% @shows.each do |show| %>
<div>
  Show: <%= show.name %>
  Genre: <%= show.genre %>
  <a href="/shows/<%= show.id %>/edit">Edit</a>
</div>
<% end %>
